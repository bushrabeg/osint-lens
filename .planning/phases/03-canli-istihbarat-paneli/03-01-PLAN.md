---
phase: 03-canli-istihbarat-paneli
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - PANEL-01
  - PANEL-03
  - PANEL-04
  - PANEL-05

must_haves:
  truths:
    - "Hedef arandıktan sonra #results içinde id='live-intel' olan panel görünür (display:block)"
    - "Panel bir 'CANLİ İSTİHBARAT' başlığı ve '// YENILE' butonu içerir"
    - "Panel içinde 'Hepsi / Reddit / RSS' filtre butonları görünür"
    - "Her kaynak satırı için yükleme durumu (yükleniyor / tamam / hata) badge HTML yapısı hazır"
    - "Panel varsayılan olarak gizli (display:none), startSearch() çağrısında gösterilir"
  artifacts:
    - path: "index.html"
      provides: "CSS rules for .live-intel-* classes"
      contains: ".live-intel-panel"
    - path: "index.html"
      provides: "#live-intel DOM element with filter buttons, status badges, refresh button, item list container"
      contains: "id=\"live-intel\""
    - path: "index.html"
      provides: "showLiveIntelPanel() function wired into startSearch()"
      contains: "showLiveIntelPanel"
  key_links:
    - from: "startSearch() setInterval else block"
      to: "showLiveIntelPanel()"
      via: "direct call after showResults(q)"
      pattern: "showLiveIntelPanel"
    - from: "#live-intel"
      to: "#results"
      via: "placed as last child inside #results div, after .tools-grid"
      pattern: "id=\"live-intel\""
---

<objective>
HTML yapısını ve CSS stillerini yazarak Canlı İstihbarat panelini index.html'e ekle. Panel araç sonuçlarının (#results içindeki .tools-grid) altına yerleştirilir.

Purpose: Phase 2'nin window.liveIntelData verisini görsel bir panele dökmek için gereken DOM iskeletini kurar. Filtre butonları, yükleme durumu badge'leri ve içerik listesi konteyneri bu planda oluşturulur; gerçek render mantığı 03-02 planında yazılır.

Output: index.html'de panel HTML'i ve CSS'i; showLiveIntelPanel() yardımcı fonksiyonu.
</objective>

<execution_context>
@/Users/busranur.begcecanli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/busranur.begcecanli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reddit-veri-cekme/01-01-SUMMARY.md

<interfaces>
<!-- Mevcut index.html yapısı — executor bu sınırları ASLA geçmemeli. -->

CSS degiskenleri (head > style icerisinden):
```css
:root {
  --bg: #080c10;
  --surface: #0d1117;
  --border: #21ce7022;
  --green: #21ce70;
  --green-dim: #21ce7066;
  --green-glow: #21ce7033;
  --red: #ff4c6a;
  --yellow: #f0c040;
  --text: #c9d1d9;
  --text-dim: #8b949e;
  --mono: 'Share Tech Mono', monospace;
  --sans: 'Rajdhani', sans-serif;
}
```

HTML ekleme noktasi (line ~501-502):
```html
    <div class="tools-grid" id="toolsGrid"></div>
  </div>   <!-- #results kapanıyor -->
```
Panel `<div class="tools-grid" ...>` ile `</div>` arasına DEGİL, toolsGrid'den SONRA, </div> kapanmadan ÖNCE eklenir:
```html
    <div class="tools-grid" id="toolsGrid"></div>
    <!-- BURAYA: <div id="live-intel" ...> </div> -->
  </div>   <!-- #results kapanıyor -->
```

startSearch() entegrasyon noktasi (setInterval else block, line ~787-792):
```javascript
renderResults(q, t ? t.results : {}, true);
// ve
document.getElementById('results').style.display='block';
```
showLiveIntelPanel() bu blogun icerisinde, results.style.display='block'tan SONRA cagrilir.

Mevcut window.liveIntelData yapisi (Phase 1 + 2 tarafindan olusturuldu):
```javascript
window.liveIntelData = {
  redditTarget: [],      // { title, subreddit, date, url }
  redditSubreddits: [],  // { title, subreddit, date, url }
  rssFeeds: [],          // { title, source, date, url }
  googleNews: [],        // { title, source, date, url }
  lastTarget: '',
  loading: {
    redditTarget: false,
    redditSubreddits: false,
    rssFeeds: false,
    googleNews: false
  }
};
```

Kaynak etiket sabitleri (03-02 ile uyumlu olmali):
- Reddit kaynaklari: redditTarget, redditSubreddits (type = 'reddit')
- RSS/News kaynaklari: rssFeeds, googleNews (type = 'rss')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Canlı İstihbarat panel CSS'ini yaz</name>
  <files>index.html</files>
  <action>
    `<style>` blogu icinde `/* RESULTS */` yorumundan ONCE, veya mevcut CSS'in en sonuna (</style> kapanmadan hemen önce) asagidaki CSS blogu ekle.

    Eklenmesi gereken CSS kurallari:

    ```css
    /* CANLI İSTİHBARAT PANEL */
    #live-intel { display: none; margin-top: 40px; border-top: 1px solid var(--border); padding-top: 32px; }
    .live-intel-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 16px; flex-wrap: wrap; gap: 8px; }
    .live-intel-title { font-family: var(--sans); font-size: 13px; letter-spacing: 3px; color: var(--green); text-transform: uppercase; }
    .live-intel-refresh-btn { background: transparent; border: 1px solid var(--green-dim); color: var(--green); font-family: var(--mono); font-size: 11px; padding: 5px 14px; cursor: pointer; letter-spacing: 2px; transition: all 0.2s; }
    .live-intel-refresh-btn:hover { background: var(--green-glow); border-color: var(--green); }
    .live-intel-refresh-btn:disabled { opacity: 0.4; cursor: not-allowed; }
    .live-intel-filters { display: flex; gap: 8px; margin-bottom: 20px; flex-wrap: wrap; }
    .live-intel-filter-btn { background: transparent; border: 1px solid #21ce7033; color: var(--text-dim); font-family: var(--mono); font-size: 11px; padding: 5px 12px; cursor: pointer; letter-spacing: 1px; transition: all 0.2s; }
    .live-intel-filter-btn:hover { border-color: var(--green-dim); color: var(--green); }
    .live-intel-filter-btn.active { border-color: var(--green); color: var(--green); background: var(--green-glow); }
    .live-intel-sources { display: flex; flex-direction: column; gap: 24px; }
    .live-intel-source-block { }
    .live-intel-source-header { display: flex; align-items: center; gap: 10px; margin-bottom: 10px; padding-bottom: 6px; border-bottom: 1px solid var(--border); }
    .live-intel-source-name { font-size: 11px; letter-spacing: 2px; color: var(--text-dim); text-transform: uppercase; }
    .live-intel-source-badge { font-size: 10px; letter-spacing: 1px; padding: 2px 8px; border: 1px solid; }
    .live-intel-source-badge.loading { color: var(--yellow); border-color: #f0c04044; background: #f0c04011; }
    .live-intel-source-badge.ok { color: var(--green); border-color: var(--green-dim); background: var(--green-glow); }
    .live-intel-source-badge.error { color: var(--red); border-color: #ff4c6a44; background: #ff4c6a11; }
    .live-intel-items { list-style: none; display: flex; flex-direction: column; gap: 6px; }
    .live-intel-item { display: flex; flex-direction: column; gap: 2px; padding: 8px 10px; border-left: 2px solid var(--border); transition: border-color 0.15s; }
    .live-intel-item:hover { border-left-color: var(--green-dim); }
    .live-intel-item a { color: var(--text); text-decoration: none; font-size: 12px; line-height: 1.4; transition: color 0.15s; }
    .live-intel-item a:hover { color: var(--green); }
    .live-intel-item-meta { font-size: 10px; color: var(--text-dim); letter-spacing: 1px; }
    .live-intel-empty { font-size: 11px; color: var(--text-dim); letter-spacing: 1px; padding: 8px 0; }
    ```

    Yalnizca bu CSS kurallari eklenir. Mevcut CSS'e dokunma.
  </action>
  <verify>grep -n "live-intel-panel\|live-intel-header\|live-intel-filter" /Users/busranur.begcecanli/Desktop/osint-lens/index.html | head -10</verify>
  <done>
    - `#live-intel { display: none; ... }` kurali index.html style blogundan grep ile bulunabilir
    - `.live-intel-filter-btn`, `.live-intel-source-badge`, `.live-intel-item` kuralları mevcut
    - Mevcut CSS kurallari bozulmamis (build browser'da acilabiliyor)
  </done>
</task>

<task type="auto">
  <name>Task 2: Panel HTML'ini #results içine ekle ve showLiveIntelPanel() fonksiyonunu yaz</name>
  <files>index.html</files>
  <action>
    **Adım 1 — HTML ekleme:**

    `<div class="tools-grid" id="toolsGrid"></div>` satirından SONRA, `</div>  <!-- #results kapaniyor -->` satirindan ÖNCE asagidaki HTML blogu ekle:

    ```html
    <!-- CANLI İSTİHBARAT PANELİ -->
    <div id="live-intel">
      <div class="live-intel-header">
        <span class="live-intel-title">// CANLİ İSTİHBARAT</span>
        <button class="live-intel-refresh-btn" id="liveIntelRefreshBtn" onclick="refreshLiveIntel()">&#8635; YENİLE</button>
      </div>
      <div class="live-intel-filters">
        <button class="live-intel-filter-btn active" onclick="setLiveIntelFilter(this,'all')">HEPSİ</button>
        <button class="live-intel-filter-btn" onclick="setLiveIntelFilter(this,'reddit')">REDDIT</button>
        <button class="live-intel-filter-btn" onclick="setLiveIntelFilter(this,'rss')">RSS / HABER</button>
      </div>
      <div class="live-intel-sources" id="liveIntelSources">
        <!-- renderLiveIntel() tarafindan doldurulacak -->
      </div>
    </div>
    ```

    **Adım 2 — showLiveIntelPanel() fonksiyonu:**

    `// ===================== CANLI İSTİHBARAT — VERİ ÇEKME =====================` section separator yorumundan ÖNCE asagidaki yeni section'i ekle:

    ```javascript
    // ===================== CANLI İSTİHBARAT — PANEL =====================

    function showLiveIntelPanel() {
      document.getElementById('live-intel').style.display = 'block';
    }
    ```

    **Adım 3 — startSearch() entegrasyonu:**

    startSearch() icindeki setInterval else block'unda `document.getElementById('results').style.display='block';` satirindan HEMEN SONRA asagidaki satiri ekle:

    ```javascript
    showLiveIntelPanel();
    ```

    Yalnizca bu uc adim uygulanir. renderLiveIntel() fonksiyonu 03-02 planina birakilir — bu planda yazilmaz.
  </action>
  <verify>grep -n "live-intel\|showLiveIntelPanel\|liveIntelRefreshBtn" /Users/busranur.begcecanli/Desktop/osint-lens/index.html | head -20</verify>
  <done>
    - `id="live-intel"` HTML elementi index.html'de mevcut ve #results div'inin icinde, tools-grid'den sonra
    - `id="liveIntelRefreshBtn"` butonu panelde mevcut
    - `.live-intel-filter-btn` filtre butonlari (HEPSİ, REDDIT, RSS / HABER) HTML'de mevcut
    - `showLiveIntelPanel()` fonksiyonu `// ===================== CANLI İSTİHBARAT — PANEL =====================` section'i altinda tanimli
    - `showLiveIntelPanel()` cagrisinin startSearch() icinde `results.style.display='block'` satirindan sonra oldugu grep ile dogrulanabilir
    - Tarayicida arama yapildiginda panel gorunuyor (bos olsa bile #live-intel blogu sayfada gorulebilir)
  </done>
</task>

</tasks>

<verification>
Her iki task tamamlandiktan sonra asagidaki kontrolleri yap:

1. `grep -n "id=\"live-intel\"" /Users/busranur.begcecanli/Desktop/osint-lens/index.html` — sonucu olmali
2. `grep -n "live-intel-filter-btn" /Users/busranur.begcecanli/Desktop/osint-lens/index.html` — en az 3 satir (CSS + 3 button)
3. `grep -n "showLiveIntelPanel" /Users/busranur.begcecanli/Desktop/osint-lens/index.html` — en az 2 satir (tanim + cagri)
4. `grep -n "liveIntelRefreshBtn" /Users/busranur.begcecanli/Desktop/osint-lens/index.html` — mevcut
5. Tarayicida index.html ac, bir hedef ara, #results gorundugunde #live-intel blogu da gorunur olmali (bos icerikle)
</verification>

<success_criteria>
- PANEL-01: `#live-intel` paneli, arama yapildiktan sonra araç sonuclarinin (toolsGrid) altinda gorunur
- PANEL-03: `liveIntelRefreshBtn` butonu DOM'da mevcut, onclick="refreshLiveIntel()" atanmis (handler 03-02'de yazilir)
- PANEL-04: `.live-intel-source-badge` CSS sinifi tanimli — loading/ok/error state'leri icin badge altyapisi hazir
- PANEL-05: HEPSİ / REDDIT / RSS filtre butonlari DOM'da mevcut, onclick="setLiveIntelFilter(this,'...')" atanmis (handler 03-02'de yazilir)
- Mevcut index.html islevi bozulmamis — arama, tab sistemi, Groq analizi, chat ayni sekilde calisıyor
</success_criteria>

<output>
Tamamlandiktan sonra `.planning/phases/03-canli-istihbarat-paneli/03-01-SUMMARY.md` dosyasini `/Users/busranur.begcecanli/.claude/get-shit-done/templates/summary.md` sablonunu kullanarak olustur.

SUMMARY su bilgileri icermeli:
- Eklenen HTML elementlerinin line numaralari
- Eklenen CSS siniflarinin listesi
- showLiveIntelPanel() fonksiyonunun imzasi ve entegrasyon noktasi
- Panel'in data-type attribute'lari (filtre icin — 03-02'nin ihtiyaci var)
- 03-02 planinin ihtiyac duydugu DOM contract (id'ler, siniflar, onclick handler isimleri)
</output>
